// All we need for v0 is 4 way movement.
enum Movement : byte {
  KeyUp,
  KeyDown,
  KeyLeft,
  KeyRight,
}

// TODO: add other action types to this (e.g. shooting).
table ActionRoot {
  movement: Movement;
}

root_type ActionRoot;

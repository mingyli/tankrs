// The definition for the base game state.

enum BlockType : byte {
  Destructible,
  Indestructible, 
}

table BlockBuf {
  x: uint16;
  y: uint16;
  block_type: BlockType;
}

table WorldBuf {
  // Dimensions of the world.
  width: uint16;
  height: uint16;
}

root_type WorldBuf;

